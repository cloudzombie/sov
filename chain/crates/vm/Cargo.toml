[package]
name = "sov-vm"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "SOV WebAssembly smart-contract runtime: a deterministic wasmi interpreter with gas-metered execution and host storage."

[dependencies]
sov-primitives = { path = "../primitives" }
wasmi = "1"
thiserror.workspace = true

[dev-dependencies]
wat = "1"

[lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"
