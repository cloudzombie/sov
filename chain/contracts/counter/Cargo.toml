[package]
name = "counter"
version = "0.0.0"
edition = "2021"
license = "Apache-2.0"
description = "Example SOV contract: a persistent counter, written with sov-sdk."

[lib]
crate-type = ["cdylib"]

[dependencies]
sov-sdk = { path = "../sov-sdk" }
