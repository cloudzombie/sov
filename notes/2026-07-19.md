# 2026-07-19

## Shipped
- **v0.1.93 — dormant cross-network replay hard fork** (commit `25b3b5d`, tag `v0.1.93`, pushed to
  `main`). Binds tx/intent signatures to `{chain_id, genesis}` behind a miner-signaled deployment
  that DEFAULTS INACTIVE. Pre-activation byte-identical; genesis `cb0272ff` frozen.
  - New `SigningDomain` in `sov-primitives`; `signing_bytes_in`/`verify_signature_in`/`sign_in`
    (tx + intent); gate via `BlockContext.tx_domain` at execution + import + mempool; `tx-domain`
    deployment reusing the PQ/BIP-8 machinery (`resolved_tx_domain`), default `None`.
  - Tests: byte-exact framing, `None==legacy`, cross-network rejection, determinism, 16-thread
    race-free verify, intent cross-network, and a full end-to-end activation-drive in `sov-chain`.
  - Also **hardened a macOS-flaky p2p test** (`stale_tip_node_finds_fork_point…`, loop ceiling
    ~15s→~60s, no assertion changed) that reddened `macos-latest` on the v0.1.92 run.
  - Proven green locally before push: full workspace tests (0 fail), KAT byte-for-byte, genesis
    pins, clippy `-D warnings`, fmt, rustdoc, node/sov-station, tools (conformance/tx-cannon/stratum).

## Decisions
- **Do NOT do a ~250-block (~10h) activation** of the tx-domain hard fork. Too aggressive for a
  hard fork on reserve mainnet (splits un-upgraded nodes; breaks legacy-signed txs at the cliff).
  Activation only on a generous horizon after all prerequisites + explicit go.
- Next build = **Phase-2 client signing** (additive, dormant, activates nothing).
- Stood up an in-repo **note system** (`notes/`): `README.md`, `STATUS.md` (master anchor), daily
  logs, and per-track activation runbooks (`activation-tx-domain.md`, `activation-pool-mining.md`).

## Verified
- Pool-mining status confirmed from `.github/RELEASE-0.1.92.md`: Phases 1–2 (getBlockTemplate/
  submitBlock RPC + sov-stratum bridge) BUILT; Phase 3 (sharechain/PPLNS) scoped; Phase 4
  (multi-output coinbase fork) disclosed, not shipped.

## Phase-2 progress
- [x] **`sov_getSigningDomain` RPC** — landed + tested (dormant → `active:false`/null). This is the
      signal the client signers read to choose bound vs legacy signing. fmt + clippy clean.
- macOS CI job on v0.1.93: **green** (last night's flaky p2p failure fixed).

## Open / next session
- [ ] Phase-2 client signers query `sov_getSigningDomain` + call `sign_in(domain)`: TS SDK (second
      client) → Rust wallet → SOV Station → conformance → tx-cannon. See `activation-tx-domain.md`.
- [ ] Confirm v0.1.93 CI fully green on GitHub (ubuntu/windows/macos build·test matrices were the
      last jobs running; everything else green).
- [ ] Run the Fable adversarial audit of `25b3b5d` once Fable quota resets (~2:40am America/Chicago).
- [ ] Pool mining: resolve the open item — how embedded PPLNS payout transfers are signed under the
      tx-domain rule (so Phase 3 and the fork don't collide). See `activation-pool-mining.md`.
